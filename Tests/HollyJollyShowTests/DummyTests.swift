import XCTest
@testable import HollyJollyShow

final class DummyTests: XCTestCase {
    func testExample() throws {
        // This is a dummy test so SwiftPM sees a Tests target.
        XCTAssertTrue(true)
    }
}